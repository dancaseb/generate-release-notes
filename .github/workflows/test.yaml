name: Unit tests
on:
  push:
    branches:
      - unittest

jobs:
  run-unit-tests:
    name: Run unit tests
    runs-on: ubuntu-latest   
    steps:
      - name: Check out code
        uses: actions/checkout@v3
      - name: setup Python
        uses: actions/setup-python@v4
      - name: Run unit tests
        run: |
          ls